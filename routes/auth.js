const express = require("express");
const router = express.Router();

// const { currentUserCtrl } = require("../controllers/auth");

// router.post("current-user", currentUserCtrl);

module.exports = router;
